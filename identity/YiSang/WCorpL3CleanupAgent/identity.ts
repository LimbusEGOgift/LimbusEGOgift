import { identityInterface } from 'identity/identity.interface';

export const identity: identityInterface = {
  name: 'WCorpL3CleanupAgent',
  trait: ['WCorp'],
  keyWord: ['Charge', 'Rupture'],
  coinNum: [1, 2, 3],
  affinity: ['Sloth', 'Glut', 'Gloom'],
  type: ['Slash', 'Pierce', 'Pierce'],
  maxWeight: 1,
  minWeight: 1,
  other: ['plusCoin', 'oneCoinReuse'],
};
