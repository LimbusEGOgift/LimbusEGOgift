import { identityInterface } from 'identity/identity.interface';

export const identity: identityInterface = {
  name: 'ThePequodFirstMate',
  trait: ['ThePequod'],
  keyWord: ['Bleed', 'Poise'],
  coinNum: [2, 2, 2],
  affinity: ['Pride', 'Envy', 'Glut'],
  type: ['Pierce', 'Pierce', 'Pierce'],
  maxWeight: 1,
  minWeight: 1,
  other: ['plusCoin'],
};
